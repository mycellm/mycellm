# mycellm — local branch triage

Snapshot taken 2026-08-15 against `main` @ `a3d8d11`
(*release: 0.7.1 — contain model download destinations*).

Regenerated by `scripts/branch-triage.sh`, the committed, read-only script this
report is derived from — re-run it for a fresh table any time.

This is a **report only**. Nothing was deleted, nothing was pushed, and no ref was
force-updated. Every deletion below is a recommendation for the human to execute.

Counts are `git rev-list --left-right --count main...<branch>` — **ahead** = commits on
the branch that `main` does not have, **behind** = commits on `main` the branch does not
have. "merged" means `git merge-base --is-ancestor <branch> main` exits 0, i.e. the
branch tip is already reachable from `main` and its residual diff vs `main` is empty.

---

## Summary

| Branch | Ahead | Behind | Status | Recommendation |
|---|---:|---:|---|---|
| `agent/announce-per-host-backoff` | 0 | 90 | merged | **delete** |
| `agent/audit-query-param-auth` | 0 | 30 | merged | **delete** |
| `agent/audit-query-param-auth-r1-df8e` | 2 | 28 | unmerged, content-equivalent | **delete** |
| `agent/audit-query-param-auth-r2-ea1e` | 0 | 28 | merged | **delete** |
| `agent/branch-triage-report` | 0 | 98 | merged | **delete** |
| `agent/capability-aware-resolution-r1-1bca` | 0 | 26 | merged | **delete** |
| `agent/capability-aware-resolution-r1-2647` | 0 | 28 | merged | **delete** |
| `agent/chat-auth-and-abort-r1-5732` | 1 | 30 | unmerged, NOTES-only | **delete** |
| `agent/chat-auth-and-abort-r2-3948` | 0 | 28 | merged | **delete** |
| `agent/chat-authed-client` | 0 | 34 | merged | **delete** |
| `agent/ci-install-training-extra` | 0 | 72 | merged | **delete** |
| `agent/fix-chat-input-hscroll` | 0 | 107 | merged | **delete** |
| `agent/fix-gitignore-logs-component` | 0 | 81 | merged | **delete** |
| `agent/fix-remote-token-leak` | 0 | 36 | merged | **delete** |
| `agent/fix-web-gitignore-logs` | 0 | 78 | merged | **delete** |
| `agent/fix-web-gitignore-logstab` | 0 | 82 | merged | **delete** |
| `agent/fleet-stale-filter` | 0 | 92 | merged | **delete** |
| `agent/guard-stale-main-index` | 0 | 0 | merged, tip == `main` | **delete** |
| `agent/hermetic-test-settings` | 0 | 80 | merged | **delete** |
| `agent/hermetic-unit-tests` | 0 | 84 | merged | **delete** |
| `agent/land-capability-role-sync` | 2 | 79 | unmerged, content-equivalent | **delete** |
| `agent/land-chat-input-hscroll` | 0 | 21 | merged | **delete** |
| `agent/land-chat-input-hscroll.sync-conflict-20260722-021319-Z4GE43I` | 1 | 103 | unmerged, content-equivalent | **delete** |
| `agent/land-distributed-training` | 0 | 65 | merged | **delete** |
| `agent/land-sse-auth-header` | 0 | 24 | merged | **delete** |
| `agent/launchd-fd-limit` | 0 | 96 | merged | **delete** |
| `agent/rebase-distributed-training` | 0 | 101 | merged | **delete** |
| `agent/rebuild-dashboard-assets` | 0 | 79 | merged | **delete** |
| `agent/refresh-branch-triage`\* | 0 | 0 | this report's working branch | **keep** until merged, then delete |
| `agent/refresh-distributed-training` | 0 | 87 | merged | **delete** |
| `agent/remote-proxy-no-token-forward` | 0 | 33 | merged | **delete** |
| `agent/repo-hygiene-stale-branches` | 2 | 83 | unmerged, NOTES-only | **delete** |
| `agent/salvage-main-worktree` | 0 | 29 | merged | **delete** |
| `agent/sse-auth-header` | 1 | 37 | unmerged, content-equivalent | **delete** |
| `agent/sse-auth-header-r1-5e51` | 0 | 28 | merged | **delete** |
| `agent/sse-auth-header-refresh` | 2 | 30 | unmerged, content-equivalent | **delete** |
| `agent/sse-auth-header-refresh-r1-42a5` | 0 | 30 | merged | **delete** |
| `agent/stream-auto-failover` | 0 | 94 | merged | **delete** |
| `agent/test-isolate-host-config` | 0 | 76 | merged | **delete** |
| `agent/triage-036eab30` | 0 | 19 | merged | **delete** |
| `agent/triage-15bf94e0` | 0 | 13 | merged | **delete** |
| `agent/triage-7d0cf846` | 0 | 19 | merged | **delete** |
| `agent/triage-ed5c210c` | 0 | 13 | merged | **delete** |
| `agent/web-credential-leaks-r1-c2aa` | 2 | 30 | unmerged, content-equivalent | **delete** |
| `agent/wire-training-dispatch-r1-a0b7` | 1 | 30 | **unmerged, real residual code** | **KEEP** |
| `chore/deps-2026-07` | 0 | 60 | merged | **delete** |
| `feat/distributed-training` | 0 | 97 | merged | **delete** |
| `feat/hardening-063` | 0 | 54 | merged | **delete** |
| `feat/memory-perf` | 0 | 48 | merged | **delete** |
| `feat/perf-features` | 0 | 44 | merged | **delete** |
| `fix/capability-role-sync` | 0 | 107 | merged | **delete** |
| `fix/embedding-model-tags` | 0 | 12 | merged | **delete** |

\* `agent/refresh-branch-triage` is the branch this report is being written on; its
counts are 0/0 at fork time and will show ahead commits once this work is committed.

52 local branches other than `main`: 43 merged (zero residual diff), 8 unmerged but
content-equivalent to `main` (safe to delete), and 1 with real unmerged code
(`agent/wire-training-dispatch-r1-a0b7` — **KEEP**).

---

## Fully merged — zero residual diff (safe to delete)

Every branch below is an ancestor of `main` (`git merge-base --is-ancestor <branch>
main` exits 0), so `git diff main...<branch>` is empty by construction. Deleting any of
these loses nothing.

`agent/guard-stale-main-index` is a special case: 0 ahead / 0 behind — its tip is
byte-identical to `main`'s current tip, a leftover duplicate ref rather than a branch
with its own history.

- `agent/announce-per-host-backoff` — 0 ahead / 90 behind `main`; ancestor of `main`, so `git diff main...agent/announce-per-host-backoff` is empty. **Delete** — fully absorbed, no unique content.
- `agent/audit-query-param-auth` — 0 ahead / 30 behind `main`; ancestor of `main`, so `git diff main...agent/audit-query-param-auth` is empty. **Delete** — fully absorbed, no unique content.
- `agent/audit-query-param-auth-r2-ea1e` — 0 ahead / 28 behind `main`; ancestor of `main`, so `git diff main...agent/audit-query-param-auth-r2-ea1e` is empty. **Delete** — fully absorbed, no unique content.
- `agent/branch-triage-report` — 0 ahead / 98 behind `main`; ancestor of `main`, so `git diff main...agent/branch-triage-report` is empty. **Delete** — fully absorbed, no unique content.
- `agent/capability-aware-resolution-r1-1bca` — 0 ahead / 26 behind `main`; ancestor of `main`, so `git diff main...agent/capability-aware-resolution-r1-1bca` is empty. **Delete** — fully absorbed, no unique content.
- `agent/capability-aware-resolution-r1-2647` — 0 ahead / 28 behind `main`; ancestor of `main`, so `git diff main...agent/capability-aware-resolution-r1-2647` is empty. **Delete** — fully absorbed, no unique content.
- `agent/chat-auth-and-abort-r2-3948` — 0 ahead / 28 behind `main`; ancestor of `main`, so `git diff main...agent/chat-auth-and-abort-r2-3948` is empty. **Delete** — fully absorbed, no unique content.
- `agent/chat-authed-client` — 0 ahead / 34 behind `main`; ancestor of `main`, so `git diff main...agent/chat-authed-client` is empty. **Delete** — fully absorbed, no unique content.
- `agent/ci-install-training-extra` — 0 ahead / 72 behind `main`; ancestor of `main`, so `git diff main...agent/ci-install-training-extra` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-chat-input-hscroll` — 0 ahead / 107 behind `main`; ancestor of `main`, so `git diff main...agent/fix-chat-input-hscroll` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-gitignore-logs-component` — 0 ahead / 81 behind `main`; ancestor of `main`, so `git diff main...agent/fix-gitignore-logs-component` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-remote-token-leak` — 0 ahead / 36 behind `main`; ancestor of `main`, so `git diff main...agent/fix-remote-token-leak` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-web-gitignore-logs` — 0 ahead / 78 behind `main`; ancestor of `main`, so `git diff main...agent/fix-web-gitignore-logs` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-web-gitignore-logstab` — 0 ahead / 82 behind `main`; ancestor of `main`, so `git diff main...agent/fix-web-gitignore-logstab` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fleet-stale-filter` — 0 ahead / 92 behind `main`; ancestor of `main`, so `git diff main...agent/fleet-stale-filter` is empty. **Delete** — fully absorbed, no unique content.
- `agent/hermetic-test-settings` — 0 ahead / 80 behind `main`; ancestor of `main`, so `git diff main...agent/hermetic-test-settings` is empty. **Delete** — fully absorbed, no unique content.
- `agent/hermetic-unit-tests` — 0 ahead / 84 behind `main`; ancestor of `main`, so `git diff main...agent/hermetic-unit-tests` is empty. **Delete** — fully absorbed, no unique content.
- `agent/land-chat-input-hscroll` — 0 ahead / 21 behind `main`; ancestor of `main`, so `git diff main...agent/land-chat-input-hscroll` is empty. **Delete** — fully absorbed, no unique content.
- `agent/land-distributed-training` — 0 ahead / 65 behind `main`; ancestor of `main`, so `git diff main...agent/land-distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `agent/land-sse-auth-header` — 0 ahead / 24 behind `main`; ancestor of `main`, so `git diff main...agent/land-sse-auth-header` is empty. **Delete** — fully absorbed, no unique content.
- `agent/launchd-fd-limit` — 0 ahead / 96 behind `main`; ancestor of `main`, so `git diff main...agent/launchd-fd-limit` is empty. **Delete** — fully absorbed, no unique content.
- `agent/rebase-distributed-training` — 0 ahead / 101 behind `main`; ancestor of `main`, so `git diff main...agent/rebase-distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `agent/rebuild-dashboard-assets` — 0 ahead / 79 behind `main`; ancestor of `main`, so `git diff main...agent/rebuild-dashboard-assets` is empty. **Delete** — fully absorbed, no unique content.
- `agent/refresh-distributed-training` — 0 ahead / 87 behind `main`; ancestor of `main`, so `git diff main...agent/refresh-distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `agent/remote-proxy-no-token-forward` — 0 ahead / 33 behind `main`; ancestor of `main`, so `git diff main...agent/remote-proxy-no-token-forward` is empty. **Delete** — fully absorbed, no unique content.
- `agent/salvage-main-worktree` — 0 ahead / 29 behind `main`; ancestor of `main`, so `git diff main...agent/salvage-main-worktree` is empty. **Delete** — fully absorbed, no unique content.
- `agent/sse-auth-header-r1-5e51` — 0 ahead / 28 behind `main`; ancestor of `main`, so `git diff main...agent/sse-auth-header-r1-5e51` is empty. **Delete** — fully absorbed, no unique content.
- `agent/sse-auth-header-refresh-r1-42a5` — 0 ahead / 30 behind `main`; ancestor of `main`, so `git diff main...agent/sse-auth-header-refresh-r1-42a5` is empty. **Delete** — fully absorbed, no unique content.
- `agent/stream-auto-failover` — 0 ahead / 94 behind `main`; ancestor of `main`, so `git diff main...agent/stream-auto-failover` is empty. **Delete** — fully absorbed, no unique content.
- `agent/test-isolate-host-config` — 0 ahead / 76 behind `main`; ancestor of `main`, so `git diff main...agent/test-isolate-host-config` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-036eab30` — 0 ahead / 19 behind `main`; ancestor of `main`, so `git diff main...agent/triage-036eab30` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-15bf94e0` — 0 ahead / 13 behind `main`; ancestor of `main`, so `git diff main...agent/triage-15bf94e0` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-7d0cf846` — 0 ahead / 19 behind `main`; ancestor of `main`, so `git diff main...agent/triage-7d0cf846` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-ed5c210c` — 0 ahead / 13 behind `main`; ancestor of `main`, so `git diff main...agent/triage-ed5c210c` is empty. **Delete** — fully absorbed, no unique content.
- `chore/deps-2026-07` — 0 ahead / 60 behind `main`; ancestor of `main`, so `git diff main...chore/deps-2026-07` is empty. **Delete** — fully absorbed, no unique content.
- `feat/distributed-training` — 0 ahead / 97 behind `main`; ancestor of `main`, so `git diff main...feat/distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `feat/hardening-063` — 0 ahead / 54 behind `main`; ancestor of `main`, so `git diff main...feat/hardening-063` is empty. **Delete** — fully absorbed, no unique content.
- `feat/memory-perf` — 0 ahead / 48 behind `main`; ancestor of `main`, so `git diff main...feat/memory-perf` is empty. **Delete** — fully absorbed, no unique content.
- `feat/perf-features` — 0 ahead / 44 behind `main`; ancestor of `main`, so `git diff main...feat/perf-features` is empty. **Delete** — fully absorbed, no unique content.
- `fix/capability-role-sync` — 0 ahead / 107 behind `main`; ancestor of `main`, so `git diff main...fix/capability-role-sync` is empty. **Delete** — fully absorbed, no unique content.
- `fix/embedding-model-tags` — 0 ahead / 12 behind `main`; ancestor of `main`, so `git diff main...fix/embedding-model-tags` is empty. **Delete** — fully absorbed, no unique content.

---

## Unmerged but content-equivalent to `main` (safe to delete)

These 8 branches are **not** ancestors of `main` — their tips carry commits `main`
never picked up verbatim — but every one either (a) touches only `NOTES.md`, this
repo's per-ticket scratch document, with no `src/`/`web/` payload, or (b) carries real
code whose functional content is already shipped on `main` under a different commit
(rebase, re-apply, or independent re-implementation). Deleting these loses no product
code; the record of *why* lives in `NOTES.md`'s triage history and in this report.

### NOTES-only — no code payload

#### `agent/chat-auth-and-abort-r1-5732` — 1 ahead / 30 behind
`git diff main...agent/chat-auth-and-abort-r1-5732 --stat` touches only `NOTES.md`
(+44 lines, a since-superseded ticket writeup). No `src/` or `web/` change.
**Delete** — process scratch, not product code.

#### `agent/repo-hygiene-stale-branches` — 2 ahead / 83 behind
Touches only `NOTES.md` (a since-superseded merge-readiness memo for
`feat/distributed-training`, now itself fully merged) and
`.drydock/branch-prune-2026-07-24.md`, an old proposal doc — no `src/` or `web/` change.
**Delete** — process scratch, not product code.

### Real code, but already shipped on `main` under a different commit

#### `agent/audit-query-param-auth-r1-df8e` — 2 ahead / 28 behind
`git diff main...agent/audit-query-param-auth-r1-df8e -- tests/unit/test_security.py`
is **empty** — the branch's characterisation tests were cherry-picked onto `main`
verbatim (see `NOTES.md`'s "Triage: agent/audit-query-param-auth-r1-df8e repo drift").
The only remaining diff is `NOTES.md`.
**Delete** — its test payload is already on `main`, byte-identical.

#### `agent/land-capability-role-sync` — 2 ahead / 79 behind
Its tip commit `6a6cfaa` ("audit all capability-role sync sites") documents in its own
message that the fix it re-audits, `f01dada` ("keep capabilities.role in sync on
runtime model load/unload"), **is already an ancestor of `main`**. Confirmed directly:
every one of the 12 `capabilities.models = node.inference.loaded_models` sites in
`src/mycellm/api/{node,models}.py` on current `main` is immediately followed by the
`capabilities.role = "seeder" if … else "consumer"` recompute the branch's audit test
checks for — the invariant the branch exists to add already holds. Residual diff is
`CHANGELOG.md` (an unreleased-section entry for a fix already folded into `main`'s
`CHANGELOG.md` under later releases) plus `NOTES.md` and the audit test.
**Delete** — superseded by `f01dada` (already on `main`).

#### `agent/land-chat-input-hscroll.sync-conflict-20260722-021319-Z4GE43I` — 1 ahead / 103 behind
Its sole code change is one Tailwind utility on `web/src/components/chat/ChatInput.tsx`
(`overflow-x-hidden` added to the `flex-1 min-w-0` textarea classes). `main`'s current
`ChatInput.tsx` already has both `min-w-0` **and** `overflow-x-hidden` on that element,
landed via merge commit `64a9a11` (*merge: land chat input horizontal-scroll fix
(e16c53f)*) — the merge-or-discard call the 2026-07-24 snapshot of this report flagged
as open has since been made, and it was "merge."
**Delete** — superseded by `64a9a11` (already on `main`).

#### `agent/sse-auth-header` — 1 ahead / 37 behind
#### `agent/sse-auth-header-refresh` — 2 ahead / 30 behind
#### `agent/web-credential-leaks-r1-c2aa` — 2 ahead / 30 behind
All three carry a version of the same fix — moving dashboard SSE auth (activity/log
streams) off an `EventSource` URL query param and onto an `Authorization: Bearer`
header — implemented independently against an older `web/src/api/client.ts` (each
branch hand-rolls its own `FetchEventSource`/`SSEConnection` wrapper using
`getHeaders()` directly). `main` shipped the same fix as **`0f97bb3`** (*fix: send SSE
auth via Authorization header instead of api_key URL param*), which routes `stream()`
through the shared `fetchWithAuth` helper (added by sibling commit `867f973`) instead of
a bare `fetch`, so the SSE path shares the 401→logout handling every other request gets
— strictly newer than what any of these three branches carry. `git merge-base
--is-ancestor 0f97bb3 main` confirms it's live on `main` today.
**Delete all three** — superseded by `0f97bb3` (already on `main`).

---

## Unmerged with real residual code — KEEP

### `agent/wire-training-dispatch-r1-a0b7` — 1 ahead / 30 behind — **KEEP, do not delete**

The only branch in this snapshot with product code that isn't already on `main` in any
form. Its one commit, `0bbb736` (*feat: dispatch TRAIN_ROUND/TRAIN_RESULT into a
node-held TrainingParticipant*), wires `node.py`'s peer-message handler to a
`TrainingParticipant` instance — the dispatch arm the distributed-training work
(`feat/distributed-training`, `agent/land-distributed-training`, now both merged) landed
the coordinator/aggregation core for but never hooked into message routing. Diff vs
`main`: `src/mycellm/node.py` (+24/−1), `tests/unit/test_training_session.py`
(+118/−1), `CHANGELOG.md` (+12).
**Keep** — this is real, unmerged functionality, pending the `land-training-dispatch`
merge-candidate proposal. Do not delete until that proposal is landed or explicitly
dropped by a human.

---

## Pruning is the human's action

Nothing in this repo — this report, `scripts/branch-triage.sh`, or any worker branch —
deletes a ref, force-updates a branch, or pushes anything. The commands below are what a
human would run to act on the **delete** recommendations above; they are listed for
reference only and were not executed as part of producing this report.

```bash
# Local branches recommended for deletion (safe: merged, or unmerged-but-superseded).
# Review the list above before running — especially the "content-equivalent" section,
# since git branch -d refuses non-ancestors and would need -D there.
git branch -d agent/announce-per-host-backoff agent/audit-query-param-auth \
  agent/audit-query-param-auth-r2-ea1e agent/branch-triage-report \
  agent/capability-aware-resolution-r1-1bca agent/capability-aware-resolution-r1-2647 \
  agent/chat-auth-and-abort-r2-3948 agent/chat-authed-client \
  agent/ci-install-training-extra agent/fix-chat-input-hscroll \
  agent/fix-gitignore-logs-component agent/fix-remote-token-leak \
  agent/fix-web-gitignore-logs agent/fix-web-gitignore-logstab \
  agent/fleet-stale-filter agent/guard-stale-main-index \
  agent/hermetic-test-settings agent/hermetic-unit-tests \
  agent/land-chat-input-hscroll agent/land-distributed-training \
  agent/land-sse-auth-header agent/launchd-fd-limit \
  agent/rebase-distributed-training agent/rebuild-dashboard-assets \
  agent/refresh-distributed-training agent/remote-proxy-no-token-forward \
  agent/salvage-main-worktree agent/sse-auth-header-r1-5e51 \
  agent/sse-auth-header-refresh-r1-42a5 agent/stream-auto-failover \
  agent/test-isolate-host-config agent/triage-036eab30 agent/triage-15bf94e0 \
  agent/triage-7d0cf846 agent/triage-ed5c210c chore/deps-2026-07 \
  feat/distributed-training feat/hardening-063 feat/memory-perf \
  feat/perf-features fix/capability-role-sync fix/embedding-model-tags

# Unmerged but content-equivalent — git branch -d will refuse these (not ancestors of
# the current branch); -D is required once the human has confirmed the "superseded by"
# commit above for each one.
git branch -D agent/audit-query-param-auth-r1-df8e agent/chat-auth-and-abort-r1-5732 \
  agent/land-capability-role-sync \
  "agent/land-chat-input-hscroll.sync-conflict-20260722-021319-Z4GE43I" \
  agent/repo-hygiene-stale-branches agent/sse-auth-header \
  agent/sse-auth-header-refresh agent/web-credential-leaks-r1-c2aa

# NOT recommended for deletion yet:
#   agent/wire-training-dispatch-r1-a0b7  — real unmerged code, KEEP
#   agent/refresh-branch-triage           — this report's own working branch

# Remote refs, if any of the above were ever pushed, are a separate, deliberate,
# network-touching operation and are explicitly out of scope for this report:
#   git push origin --delete <branch>
```
