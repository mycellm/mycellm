# mycellm — local branch triage

Snapshot taken 2026-08-15 against `main` @ `f6f552e`
(*deliver: Add a read-only preflight that detects the recurring stale-index state in
the main checkout (exec 2aeea343)*).

Regenerated by `scripts/branch-triage.sh`, the committed, read-only script this
report is derived from — re-run it for a fresh table any time.

This is a **report only**. Nothing was deleted, nothing was pushed, and no ref was
force-updated. Every deletion below is a recommendation for the human to execute.

Counts are `git rev-list --left-right --count main...<branch>` — **ahead** = commits on
the branch that `main` does not have, **behind** = commits on `main` the branch does not
have. "merged" means `git merge-base --is-ancestor <branch> main` exits 0, i.e. the
branch tip is already reachable from `main` and its residual diff vs `main` is empty.

---

## Summary

| Branch | Ahead | Behind | Status | Recommendation |
|---|---:|---:|---|---|
| `agent/announce-per-host-backoff` | 0 | 92 | merged | **delete** |
| `agent/audit-query-param-auth` | 0 | 32 | merged | **delete** |
| `agent/audit-query-param-auth-r1-df8e` | 2 | 30 | unmerged, content-equivalent | **delete** |
| `agent/audit-query-param-auth-r2-ea1e` | 0 | 30 | merged | **delete** |
| `agent/branch-triage-report` | 0 | 100 | merged | **delete** |
| `agent/capability-aware-resolution-r1-1bca` | 0 | 28 | merged | **delete** |
| `agent/capability-aware-resolution-r1-2647` | 0 | 30 | merged | **delete** |
| `agent/chat-auth-and-abort-r1-5732` | 1 | 32 | unmerged, NOTES-only | **delete** |
| `agent/chat-auth-and-abort-r2-3948` | 0 | 30 | merged | **delete** |
| `agent/chat-authed-client` | 0 | 36 | merged | **delete** |
| `agent/ci-install-training-extra` | 0 | 74 | merged | **delete** |
| `agent/fix-chat-input-hscroll` | 0 | 109 | merged | **delete** |
| `agent/fix-gitignore-logs-component` | 0 | 83 | merged | **delete** |
| `agent/fix-remote-token-leak` | 0 | 38 | merged | **delete** |
| `agent/fix-web-gitignore-logs` | 0 | 80 | merged | **delete** |
| `agent/fix-web-gitignore-logstab` | 0 | 84 | merged | **delete** |
| `agent/fleet-stale-filter` | 0 | 94 | merged | **delete** |
| `agent/guard-stale-main-index` | 0 | 1 | merged | **delete** |
| `agent/hermetic-test-settings` | 0 | 82 | merged | **delete** |
| `agent/hermetic-unit-tests` | 0 | 86 | merged | **delete** |
| `agent/land-capability-role-sync` | 2 | 81 | unmerged, content-equivalent | **delete** |
| `agent/land-chat-input-hscroll` | 0 | 23 | merged | **delete** |
| `agent/land-chat-input-hscroll.sync-conflict-20260722-021319-Z4GE43I` | 1 | 105 | unmerged, content-equivalent | **delete** |
| `agent/land-distributed-training` | 0 | 67 | merged | **delete** |
| `agent/land-sse-auth-header` | 0 | 26 | merged | **delete** |
| `agent/launchd-fd-limit` | 0 | 98 | merged | **delete** |
| `agent/rebase-distributed-training` | 0 | 103 | merged | **delete** |
| `agent/rebuild-dashboard-assets` | 0 | 81 | merged | **delete** |
| `agent/refresh-branch-triage`\* | 2 | 2 | unmerged, this report's working branch | **keep** until merged, then delete |
| `agent/refresh-distributed-training` | 0 | 89 | merged | **delete** |
| `agent/remote-proxy-no-token-forward` | 0 | 35 | merged | **delete** |
| `agent/repo-hygiene-stale-branches` | 2 | 85 | unmerged, NOTES-only | **delete** |
| `agent/salvage-main-worktree` | 0 | 31 | merged | **delete** |
| `agent/sse-auth-header` | 1 | 39 | unmerged, content-equivalent | **delete** |
| `agent/sse-auth-header-r1-5e51` | 0 | 30 | merged | **delete** |
| `agent/sse-auth-header-refresh` | 2 | 32 | unmerged, content-equivalent | **delete** |
| `agent/sse-auth-header-refresh-r1-42a5` | 0 | 32 | merged | **delete** |
| `agent/stream-auto-failover` | 0 | 96 | merged | **delete** |
| `agent/test-isolate-host-config` | 0 | 78 | merged | **delete** |
| `agent/triage-036eab30` | 0 | 21 | merged | **delete** |
| `agent/triage-15bf94e0` | 0 | 15 | merged | **delete** |
| `agent/triage-7d0cf846` | 0 | 21 | merged | **delete** |
| `agent/triage-ed5c210c` | 0 | 15 | merged | **delete** |
| `agent/web-credential-leaks-r1-c2aa` | 2 | 32 | unmerged, content-equivalent | **delete** |
| `agent/wire-training-dispatch-r1-a0b7` | 1 | 32 | **unmerged, real residual code** | **KEEP** |
| `chore/deps-2026-07` | 0 | 62 | merged | **delete** |
| `feat/distributed-training` | 0 | 99 | merged | **delete** |
| `feat/hardening-063` | 0 | 56 | merged | **delete** |
| `feat/memory-perf` | 0 | 50 | merged | **delete** |
| `feat/perf-features` | 0 | 46 | merged | **delete** |
| `fix/capability-role-sync` | 0 | 109 | merged | **delete** |
| `fix/embedding-model-tags` | 0 | 14 | merged | **delete** |

\* `agent/refresh-branch-triage` is the branch this report is being written on. Its
counts are stated as of the commit that lands this snapshot: 2 ahead (the script and
this report) and 2 behind (`main` picked up `ca10de9` and `f6f552e` after this branch
forked). It is not merged and is not a deletion candidate until it lands.

52 local branches other than `main`: 42 merged (zero residual diff), 8 unmerged but
content-equivalent to `main` (safe to delete), 1 with real unmerged code
(`agent/wire-training-dispatch-r1-a0b7` — **KEEP**), and this report's own working
branch (`agent/refresh-branch-triage` — **KEEP** until merged).

---

## Fully merged — zero residual diff (safe to delete)

Every branch below is an ancestor of `main` (`git merge-base --is-ancestor <branch>
main` exits 0), so `git diff main...<branch>` is empty by construction. Deleting any of
these loses nothing.

`agent/guard-stale-main-index` is the freshest of these: 0 ahead / 1 behind — its work
landed as `ca10de9`, and `main` has moved exactly one commit past it (`f6f552e`). The
ref is now a leftover pointer into `main`'s history rather than a branch with unique
content.

- `agent/announce-per-host-backoff` — 0 ahead / 92 behind `main`; ancestor of `main`, so `git diff main...agent/announce-per-host-backoff` is empty. **Delete** — fully absorbed, no unique content.
- `agent/audit-query-param-auth` — 0 ahead / 32 behind `main`; ancestor of `main`, so `git diff main...agent/audit-query-param-auth` is empty. **Delete** — fully absorbed, no unique content.
- `agent/audit-query-param-auth-r2-ea1e` — 0 ahead / 30 behind `main`; ancestor of `main`, so `git diff main...agent/audit-query-param-auth-r2-ea1e` is empty. **Delete** — fully absorbed, no unique content.
- `agent/branch-triage-report` — 0 ahead / 100 behind `main`; ancestor of `main`, so `git diff main...agent/branch-triage-report` is empty. **Delete** — fully absorbed, no unique content.
- `agent/capability-aware-resolution-r1-1bca` — 0 ahead / 28 behind `main`; ancestor of `main`, so `git diff main...agent/capability-aware-resolution-r1-1bca` is empty. **Delete** — fully absorbed, no unique content.
- `agent/capability-aware-resolution-r1-2647` — 0 ahead / 30 behind `main`; ancestor of `main`, so `git diff main...agent/capability-aware-resolution-r1-2647` is empty. **Delete** — fully absorbed, no unique content.
- `agent/chat-auth-and-abort-r2-3948` — 0 ahead / 30 behind `main`; ancestor of `main`, so `git diff main...agent/chat-auth-and-abort-r2-3948` is empty. **Delete** — fully absorbed, no unique content.
- `agent/chat-authed-client` — 0 ahead / 36 behind `main`; ancestor of `main`, so `git diff main...agent/chat-authed-client` is empty. **Delete** — fully absorbed, no unique content.
- `agent/ci-install-training-extra` — 0 ahead / 74 behind `main`; ancestor of `main`, so `git diff main...agent/ci-install-training-extra` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-chat-input-hscroll` — 0 ahead / 109 behind `main`; ancestor of `main`, so `git diff main...agent/fix-chat-input-hscroll` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-gitignore-logs-component` — 0 ahead / 83 behind `main`; ancestor of `main`, so `git diff main...agent/fix-gitignore-logs-component` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-remote-token-leak` — 0 ahead / 38 behind `main`; ancestor of `main`, so `git diff main...agent/fix-remote-token-leak` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-web-gitignore-logs` — 0 ahead / 80 behind `main`; ancestor of `main`, so `git diff main...agent/fix-web-gitignore-logs` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fix-web-gitignore-logstab` — 0 ahead / 84 behind `main`; ancestor of `main`, so `git diff main...agent/fix-web-gitignore-logstab` is empty. **Delete** — fully absorbed, no unique content.
- `agent/fleet-stale-filter` — 0 ahead / 94 behind `main`; ancestor of `main`, so `git diff main...agent/fleet-stale-filter` is empty. **Delete** — fully absorbed, no unique content.
- `agent/hermetic-test-settings` — 0 ahead / 82 behind `main`; ancestor of `main`, so `git diff main...agent/hermetic-test-settings` is empty. **Delete** — fully absorbed, no unique content.
- `agent/hermetic-unit-tests` — 0 ahead / 86 behind `main`; ancestor of `main`, so `git diff main...agent/hermetic-unit-tests` is empty. **Delete** — fully absorbed, no unique content.
- `agent/land-chat-input-hscroll` — 0 ahead / 23 behind `main`; ancestor of `main`, so `git diff main...agent/land-chat-input-hscroll` is empty. **Delete** — fully absorbed, no unique content.
- `agent/land-distributed-training` — 0 ahead / 67 behind `main`; ancestor of `main`, so `git diff main...agent/land-distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `agent/land-sse-auth-header` — 0 ahead / 26 behind `main`; ancestor of `main`, so `git diff main...agent/land-sse-auth-header` is empty. **Delete** — fully absorbed, no unique content.
- `agent/launchd-fd-limit` — 0 ahead / 98 behind `main`; ancestor of `main`, so `git diff main...agent/launchd-fd-limit` is empty. **Delete** — fully absorbed, no unique content.
- `agent/rebase-distributed-training` — 0 ahead / 103 behind `main`; ancestor of `main`, so `git diff main...agent/rebase-distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `agent/rebuild-dashboard-assets` — 0 ahead / 81 behind `main`; ancestor of `main`, so `git diff main...agent/rebuild-dashboard-assets` is empty. **Delete** — fully absorbed, no unique content.
- `agent/refresh-distributed-training` — 0 ahead / 89 behind `main`; ancestor of `main`, so `git diff main...agent/refresh-distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `agent/remote-proxy-no-token-forward` — 0 ahead / 35 behind `main`; ancestor of `main`, so `git diff main...agent/remote-proxy-no-token-forward` is empty. **Delete** — fully absorbed, no unique content.
- `agent/salvage-main-worktree` — 0 ahead / 31 behind `main`; ancestor of `main`, so `git diff main...agent/salvage-main-worktree` is empty. **Delete** — fully absorbed, no unique content.
- `agent/sse-auth-header-r1-5e51` — 0 ahead / 30 behind `main`; ancestor of `main`, so `git diff main...agent/sse-auth-header-r1-5e51` is empty. **Delete** — fully absorbed, no unique content.
- `agent/sse-auth-header-refresh-r1-42a5` — 0 ahead / 32 behind `main`; ancestor of `main`, so `git diff main...agent/sse-auth-header-refresh-r1-42a5` is empty. **Delete** — fully absorbed, no unique content.
- `agent/stream-auto-failover` — 0 ahead / 96 behind `main`; ancestor of `main`, so `git diff main...agent/stream-auto-failover` is empty. **Delete** — fully absorbed, no unique content.
- `agent/test-isolate-host-config` — 0 ahead / 78 behind `main`; ancestor of `main`, so `git diff main...agent/test-isolate-host-config` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-036eab30` — 0 ahead / 21 behind `main`; ancestor of `main`, so `git diff main...agent/triage-036eab30` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-15bf94e0` — 0 ahead / 15 behind `main`; ancestor of `main`, so `git diff main...agent/triage-15bf94e0` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-7d0cf846` — 0 ahead / 21 behind `main`; ancestor of `main`, so `git diff main...agent/triage-7d0cf846` is empty. **Delete** — fully absorbed, no unique content.
- `agent/triage-ed5c210c` — 0 ahead / 15 behind `main`; ancestor of `main`, so `git diff main...agent/triage-ed5c210c` is empty. **Delete** — fully absorbed, no unique content.
- `chore/deps-2026-07` — 0 ahead / 62 behind `main`; ancestor of `main`, so `git diff main...chore/deps-2026-07` is empty. **Delete** — fully absorbed, no unique content.
- `feat/distributed-training` — 0 ahead / 99 behind `main`; ancestor of `main`, so `git diff main...feat/distributed-training` is empty. **Delete** — fully absorbed, no unique content.
- `feat/hardening-063` — 0 ahead / 56 behind `main`; ancestor of `main`, so `git diff main...feat/hardening-063` is empty. **Delete** — fully absorbed, no unique content.
- `feat/memory-perf` — 0 ahead / 50 behind `main`; ancestor of `main`, so `git diff main...feat/memory-perf` is empty. **Delete** — fully absorbed, no unique content.
- `feat/perf-features` — 0 ahead / 46 behind `main`; ancestor of `main`, so `git diff main...feat/perf-features` is empty. **Delete** — fully absorbed, no unique content.
- `fix/capability-role-sync` — 0 ahead / 109 behind `main`; ancestor of `main`, so `git diff main...fix/capability-role-sync` is empty. **Delete** — fully absorbed, no unique content.
- `fix/embedding-model-tags` — 0 ahead / 14 behind `main`; ancestor of `main`, so `git diff main...fix/embedding-model-tags` is empty. **Delete** — fully absorbed, no unique content.

---

## Unmerged but content-equivalent to `main` (safe to delete)

These 8 branches are **not** ancestors of `main` — their tips carry commits `main`
never picked up verbatim — but every one either (a) touches only `NOTES.md`, this
repo's per-ticket scratch document, with no `src/`/`web/` payload, or (b) carries real
code whose functional content is already shipped on `main` under a different commit
(rebase, re-apply, or independent re-implementation). Deleting these loses no product
code; the record of *why* lives in `NOTES.md`'s triage history and in this report.

### NOTES-only — no code payload

#### `agent/chat-auth-and-abort-r1-5732` — 1 ahead / 32 behind
`git diff main...agent/chat-auth-and-abort-r1-5732 --stat` touches only `NOTES.md`
(+44 lines, a since-superseded ticket writeup). No `src/` or `web/` change.
**Delete** — process scratch, not product code.

#### `agent/repo-hygiene-stale-branches` — 2 ahead / 85 behind
Touches only `NOTES.md` (a since-superseded merge-readiness memo for
`feat/distributed-training`, now itself fully merged) and
`.drydock/branch-prune-2026-07-24.md`, an old proposal doc — no `src/` or `web/` change.
**Delete** — process scratch, not product code.

### Real code, but already shipped on `main` under a different commit

#### `agent/audit-query-param-auth-r1-df8e` — 2 ahead / 30 behind
`git diff main...agent/audit-query-param-auth-r1-df8e -- tests/unit/test_security.py`
is **empty** — the branch's characterisation tests were cherry-picked onto `main`
verbatim (see `NOTES.md`'s "Triage: agent/audit-query-param-auth-r1-df8e repo drift").
The only remaining diff is `NOTES.md`.
**Delete** — its test payload is already on `main`, byte-identical.

#### `agent/land-capability-role-sync` — 2 ahead / 81 behind
Its tip commit `6a6cfaa` ("audit all capability-role sync sites") documents in its own
message that the fix it re-audits, `f01dada` ("keep capabilities.role in sync on
runtime model load/unload"), **is already an ancestor of `main`**. Confirmed directly:
every one of the 12 `capabilities.models = node.inference.loaded_models` sites in
`src/mycellm/api/{node,models}.py` on current `main` is immediately followed by the
`capabilities.role = "seeder" if … else "consumer"` recompute the branch's audit test
checks for — the invariant the branch exists to add already holds. Residual diff is
`CHANGELOG.md` (an unreleased-section entry for a fix already folded into `main`'s
`CHANGELOG.md` under later releases) plus `NOTES.md` and the audit test.
**Delete** — superseded by `f01dada` (already on `main`).

#### `agent/land-chat-input-hscroll.sync-conflict-20260722-021319-Z4GE43I` — 1 ahead / 105 behind
Its sole code change is one Tailwind utility on `web/src/components/chat/ChatInput.tsx`
(`overflow-x-hidden` added to the `flex-1 min-w-0` textarea classes). `main`'s current
`ChatInput.tsx` already has both `min-w-0` **and** `overflow-x-hidden` on that element,
landed via merge commit `64a9a11` (*merge: land chat input horizontal-scroll fix
(e16c53f)*) — the merge-or-discard call the 2026-07-24 snapshot of this report flagged
as open has since been made, and it was "merge."
**Delete** — superseded by `64a9a11` (already on `main`).

#### `agent/sse-auth-header` — 1 ahead / 39 behind
#### `agent/sse-auth-header-refresh` — 2 ahead / 32 behind
#### `agent/web-credential-leaks-r1-c2aa` — 2 ahead / 32 behind
All three carry a version of the same fix — moving dashboard SSE auth (activity/log
streams) off an `EventSource` URL query param and onto an `Authorization: Bearer`
header — implemented independently against an older `web/src/api/client.ts` (each
branch hand-rolls its own `FetchEventSource`/`SSEConnection` wrapper using
`getHeaders()` directly). `main` shipped the same fix as **`0f97bb3`** (*fix: send SSE
auth via Authorization header instead of api_key URL param*), which routes `stream()`
through the shared `fetchWithAuth` helper (added by sibling commit `867f973`) instead of
a bare `fetch`, so the SSE path shares the 401→logout handling every other request gets
— strictly newer than what any of these three branches carry. `git merge-base
--is-ancestor 0f97bb3 main` confirms it's live on `main` today.
**Delete all three** — superseded by `0f97bb3` (already on `main`).

---

## Unmerged with real residual code — KEEP

### `agent/wire-training-dispatch-r1-a0b7` — 1 ahead / 32 behind — **KEEP, do not delete**

The only branch in this snapshot with product code that isn't already on `main` in any
form. Its one commit, `0bbb736` (*feat: dispatch TRAIN_ROUND/TRAIN_RESULT into a
node-held TrainingParticipant*), wires `node.py`'s peer-message handler to a
`TrainingParticipant` instance — the dispatch arm the distributed-training work
(`feat/distributed-training`, `agent/land-distributed-training`, now both merged) landed
the coordinator/aggregation core for but never hooked into message routing. Diff vs
`main`: `src/mycellm/node.py` (+22/−2), `tests/unit/test_training_session.py`
(+118/−1), `CHANGELOG.md` (+12).
**Keep** — this is real, unmerged functionality, pending the `land-training-dispatch`
merge-candidate proposal. Do not delete until that proposal is landed or explicitly
dropped by a human.

---

## Pruning is the human's action

Nothing in this repo — this report, `scripts/branch-triage.sh`, or any worker branch —
deletes a ref, force-updates a branch, or pushes anything. The commands below are what a
human would run to act on the **delete** recommendations above; they are listed for
reference only and were not executed as part of producing this report.

```bash
# Local branches recommended for deletion (safe: merged, or unmerged-but-superseded).
# Review the list above before running — especially the "content-equivalent" section,
# since git branch -d refuses non-ancestors and would need -D there.
git branch -d agent/announce-per-host-backoff agent/audit-query-param-auth \
  agent/audit-query-param-auth-r2-ea1e agent/branch-triage-report \
  agent/capability-aware-resolution-r1-1bca agent/capability-aware-resolution-r1-2647 \
  agent/chat-auth-and-abort-r2-3948 agent/chat-authed-client \
  agent/ci-install-training-extra agent/fix-chat-input-hscroll \
  agent/fix-gitignore-logs-component agent/fix-remote-token-leak \
  agent/fix-web-gitignore-logs agent/fix-web-gitignore-logstab \
  agent/fleet-stale-filter agent/guard-stale-main-index \
  agent/hermetic-test-settings agent/hermetic-unit-tests \
  agent/land-chat-input-hscroll agent/land-distributed-training \
  agent/land-sse-auth-header agent/launchd-fd-limit \
  agent/rebase-distributed-training agent/rebuild-dashboard-assets \
  agent/refresh-distributed-training agent/remote-proxy-no-token-forward \
  agent/salvage-main-worktree agent/sse-auth-header-r1-5e51 \
  agent/sse-auth-header-refresh-r1-42a5 agent/stream-auto-failover \
  agent/test-isolate-host-config agent/triage-036eab30 agent/triage-15bf94e0 \
  agent/triage-7d0cf846 agent/triage-ed5c210c chore/deps-2026-07 \
  feat/distributed-training feat/hardening-063 feat/memory-perf \
  feat/perf-features fix/capability-role-sync fix/embedding-model-tags

# Unmerged but content-equivalent — git branch -d will refuse these (not ancestors of
# the current branch); -D is required once the human has confirmed the "superseded by"
# commit above for each one.
git branch -D agent/audit-query-param-auth-r1-df8e agent/chat-auth-and-abort-r1-5732 \
  agent/land-capability-role-sync \
  "agent/land-chat-input-hscroll.sync-conflict-20260722-021319-Z4GE43I" \
  agent/repo-hygiene-stale-branches agent/sse-auth-header \
  agent/sse-auth-header-refresh agent/web-credential-leaks-r1-c2aa

# NOT recommended for deletion yet:
#   agent/wire-training-dispatch-r1-a0b7  — real unmerged code, KEEP
#   agent/refresh-branch-triage           — this report's own working branch

# Remote refs, if any of the above were ever pushed, are a separate, deliberate,
# network-touching operation and are explicitly out of scope for this report:
#   git push origin --delete <branch>
```
