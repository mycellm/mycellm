# Lint and test on push/PR. Fast path: unit, integration, and harness smoke tests.
# Full multi-node E2E (spawns real `mycellm serve` on localhost) runs after that succeeds.
name: Lint and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install web dependencies
        working-directory: web
        run: npm ci

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and dev tools
        run: pip install -e ".[dev]"

      - name: Ruff check
        run: ruff check src tests

      - name: ESLint (web)
        working-directory: web
        run: npm run lint

      - name: Pytest (unit, integration, harness smoke)
        run: >
          pytest tests/unit tests/integration tests/e2e/test_harness.py
          -q --tb=short

  e2e:
    needs: lint-and-test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and dev tools
        run: pip install -e ".[dev]"

      - name: Pytest (multi-node E2E)
        run: pytest tests/e2e/test_three_nodes.py -v --tb=short
